/* Generated by @deepseek-ai/dsh-typert-generator from FaceModel — do not edit. */
import { z } from 'zod'

const _deepseek_ai_dsh_credentials_credentials_describe_parameter_0$schema = z.array(z.string())
const _deepseek_ai_dsh_credentials_credentials_describe_result$schema = z.object({
  'credentials': z.record(z.string(), z.object({
  'configured': z.boolean().readonly(),
  'source': z.string().readonly().optional(),
  'writable': z.boolean().readonly(),
})).readonly().readonly(),
})
const _deepseek_ai_dsh_credentials_credentials_set_parameter_0$schema = z.string()
const _deepseek_ai_dsh_credentials_credentials_set_parameter_1$schema = z.string()
const _deepseek_ai_dsh_credentials_credentials_set_result$schema = z.object({})
const _deepseek_ai_dsh_credentials_credentials_unset_parameter_0$schema = z.string()
const _deepseek_ai_dsh_credentials_credentials_unset_result$schema = z.object({})

export const TYPERT = {
  package: '@deepseek-ai/dsh-credentials',
  face: 'host',
  schemas: [
  ],
  invocations: [
    {
      id: '@deepseek-ai/dsh-credentials#credentials/describe',
      service: 'credentials',
      namespace: 'credentials',
      method: 'describe',
      implementation: 'remoteDescribe',
      invocation: { kind: 'direct' },
      parameters: [
        {
          name: 'refs',
          wire: 'refs',
          source: 'json',
          codec: {
            mode: 'strict',
            typeSymbol: '@deepseek-ai/dsh-credentials#credentials/describe:refs',
            schema: _deepseek_ai_dsh_credentials_credentials_describe_parameter_0$schema,
          },
        },
      ],
      result: {
        mode: 'strict',
        typeSymbol: '@deepseek-ai/dsh-credentials/types#RemoteCredentialsDescription',
        schema: _deepseek_ai_dsh_credentials_credentials_describe_result$schema,
      },
      sourceLocation: {"file":"packages/credentials/credentials/src/index.ts","line":288,"column":9},
    },
    {
      id: '@deepseek-ai/dsh-credentials#credentials/set',
      service: 'credentials',
      namespace: 'credentials',
      method: 'set',
      implementation: 'remoteSet',
      invocation: { kind: 'direct' },
      parameters: [
        {
          name: 'refName',
          wire: 'refName',
          source: 'json',
          codec: {
            mode: 'strict',
            typeSymbol: '@deepseek-ai/dsh-credentials#credentials/set:refName',
            schema: _deepseek_ai_dsh_credentials_credentials_set_parameter_0$schema,
          },
        },
        {
          name: 'value',
          wire: 'value',
          source: 'json',
          codec: {
            mode: 'strict',
            typeSymbol: '@deepseek-ai/dsh-credentials#credentials/set:value',
            schema: _deepseek_ai_dsh_credentials_credentials_set_parameter_1$schema,
          },
        },
      ],
      result: {
        mode: 'strict',
        typeSymbol: '@deepseek-ai/dsh-credentials#credentials/set:result',
        schema: _deepseek_ai_dsh_credentials_credentials_set_result$schema,
      },
      sourceLocation: {"file":"packages/credentials/credentials/src/index.ts","line":318,"column":9},
    },
    {
      id: '@deepseek-ai/dsh-credentials#credentials/unset',
      service: 'credentials',
      namespace: 'credentials',
      method: 'unset',
      implementation: 'remoteUnset',
      invocation: { kind: 'direct' },
      parameters: [
        {
          name: 'refName',
          wire: 'refName',
          source: 'json',
          codec: {
            mode: 'strict',
            typeSymbol: '@deepseek-ai/dsh-credentials#credentials/unset:refName',
            schema: _deepseek_ai_dsh_credentials_credentials_unset_parameter_0$schema,
          },
        },
      ],
      result: {
        mode: 'strict',
        typeSymbol: '@deepseek-ai/dsh-credentials#credentials/unset:result',
        schema: _deepseek_ai_dsh_credentials_credentials_unset_result$schema,
      },
      sourceLocation: {"file":"packages/credentials/credentials/src/index.ts","line":339,"column":9},
    },
  ],
  model: {
    "services": [
      {
        "description": "Abstract credential service over two key spaces that answer two questions. A {@link CredentialRef} answers \"what is behind this environment-variable name\", layered over the process environment, the provider-managed store, and `.env` files. One seam-wide rule binds that half: an empty stored value is absent everywhere — `resolve` skips it, `describe` reports it unconfigured — so a blank never masquerades as a configured secret. A {@link CredentialKey} answers \"what credential does this plugin hold for this id\". Nothing can layer here — an authorization grant has no environment to be read from — so presence of the record is the whole fact, and {@link modifyRecord} is the only write path because a correct write depends on the current value (a token refresh is read-decide-replace under one lock).",
        "summary": "Abstract credential service over two key spaces that answer two questions.",
        "tags": [],
        "jsDoc": "/**\n * Abstract credential service over two key spaces that answer two questions.\n *\n * A {@link CredentialRef} answers \"what is behind this environment-variable\n * name\", layered over the process environment, the provider-managed store, and\n * `.env` files. One seam-wide rule binds that half: an empty stored value is\n * absent everywhere — `resolve` skips it, `describe` reports it unconfigured —\n * so a blank never masquerades as a configured secret.\n *\n * A {@link CredentialKey} answers \"what credential does this plugin hold for\n * this id\". Nothing can layer here — an authorization grant has no\n * environment to be read from — so presence of the record is the whole fact,\n * and {@link modifyRecord} is the only write path because a correct write\n * depends on the current value (a token refresh is read-decide-replace under\n * one lock).\n */",
        "key": "credentials",
        "exportName": "CredentialProvider",
        "members": [
          {
            "kind": "method",
            "name": "resolve",
            "signature": "abstract resolve(ref: CredentialRef): Promise<ResolvedCredential | undefined>",
            "summary": "Resolve one reference to its current value.",
            "jsDoc": "/**\n * Resolve one reference to its current value. Resolution is per call:\n * consumers re-resolve at each operation and must not cache across\n * operations — that per-operation read is what makes a changed credential\n * reach the next operation without a restart.\n * @param ref - the reference to resolve.\n * @returns the value and its source, or `undefined` while unconfigured.\n */"
          },
          {
            "kind": "method",
            "name": "describe",
            "signature": "abstract describe(ref: CredentialRef): Promise<CredentialInfo>",
            "summary": "Describe one reference for configuration surfaces without exposing the value.",
            "jsDoc": "/**\n * Describe one reference for configuration surfaces without exposing the\n * value.\n * @param ref - the reference to describe.\n * @returns configured state, supplying source, and writability.\n */"
          },
          {
            "kind": "method",
            "name": "set",
            "signature": "abstract set(ref: CredentialRef, value: string): Promise<void>",
            "summary": "Durably store one value in the provider-managed writable source.",
            "jsDoc": "/**\n * Durably store one value in the provider-managed writable source. Rejects\n * while a read-only source shadows the reference — the write would appear\n * to succeed while resolution keeps returning the shadowing value — and\n * rejects an empty value (use {@link unset}).\n * @param ref - the reference to store.\n * @param value - the non-empty secret value.\n */"
          },
          {
            "kind": "method",
            "name": "unset",
            "signature": "abstract unset(ref: CredentialRef): Promise<void>",
            "summary": "Remove one reference from the provider-managed writable source; removing an absent reference is a no-op.",
            "jsDoc": "/**\n * Remove one reference from the provider-managed writable source; removing\n * an absent reference is a no-op. Rejects while a read-only source shadows\n * the reference, like {@link set}.\n * @param ref - the reference to remove.\n */"
          },
          {
            "kind": "method",
            "name": "readRecord",
            "signature": "abstract readRecord(key: CredentialKey): Promise<CredentialRecord | undefined>",
            "summary": "Read one stored record.",
            "jsDoc": "/**\n * Read one stored record. The value is returned as its owner wrote it; a\n * {@link GrantRecord} payload is not interpreted on the way out.\n * @param key - the record to read.\n * @returns the record, or `undefined` while none is stored.\n */"
          },
          {
            "kind": "method",
            "name": "describeRecord",
            "signature": "abstract describeRecord(key: CredentialKey): Promise<CredentialRecordInfo>",
            "summary": "Describe one record for configuration surfaces without exposing its value.",
            "jsDoc": "/**\n * Describe one record for configuration surfaces without exposing its value.\n * @param key - the record to describe.\n * @returns presence, discriminant, and writability.\n */"
          },
          {
            "kind": "method",
            "name": "listRecords",
            "signature": "abstract listRecords(): Promise<readonly CredentialRecordEntry[]>",
            "summary": "Enumerate every stored record's address and tag.",
            "jsDoc": "/**\n * Enumerate every stored record's address and tag. Unlike the reference\n * half, which has no enumeration because configuration surfaces learn which\n * references exist from settings schemas, records have no such discovery\n * path: a surface that cannot list them cannot show what a user is\n * authorized for, nor find an orphan left by an uninstalled plugin.\n * @returns every stored record, values excluded.\n */"
          },
          {
            "kind": "method",
            "name": "modifyRecord",
            "signature": "abstract modifyRecord( key: CredentialKey, mutate: (current: CredentialRecord | undefined) => Promise<CredentialRecord | undefined>, ): Promise<CredentialRecord | undefined>",
            "summary": "Serialized read-modify-write over one record — the only write path.",
            "jsDoc": "/**\n * Serialized read-modify-write over one record — the only write path.\n * `mutate` sees the record as it stands at the moment the write is\n * exclusive, and returning `undefined` leaves the entry untouched. Exclusion\n * holds across processes where the backing store supports it, which is what\n * makes a token refresh safe: two processes rotating one refresh token\n * concurrently would otherwise lose whichever wrote first.\n * @param key - the record to modify.\n * @param mutate - receives the current record and returns its replacement, or `undefined` to leave it.\n * @returns the record after the write, or the current one when `mutate` declined.\n */"
          },
          {
            "kind": "method",
            "name": "deleteRecord",
            "signature": "abstract deleteRecord(key: CredentialKey): Promise<void>",
            "summary": "Remove one record; removing an absent record is a no-op.",
            "jsDoc": "/**\n * Remove one record; removing an absent record is a no-op.\n * @param key - the record to remove.\n */"
          },
          {
            "kind": "method",
            "name": "remoteDescribe",
            "signature": "@Remote('describe') async remoteDescribe(refs: readonly string[]): Promise<RemoteCredentialsDescription>",
            "summary": "Describe named references without returning any credential value.",
            "jsDoc": "/**\n * Describe named references without returning any credential value.\n * There is intentionally no Remote enumeration endpoint: settings schemas\n * remain the authority that tells a configuration surface which refs exist.\n * @param refs - credential reference names to describe.\n * @returns redacted metadata for each requested reference.\n */"
          },
          {
            "kind": "method",
            "name": "remoteSet",
            "signature": "@Remote('set') async remoteSet(refName: string, value: string): Promise<Record<never, never>>",
            "summary": "Store one write-only credential value through the Native Remote plane.",
            "jsDoc": "/**\n * Store one write-only credential value through the Native Remote plane.\n * @param refName - credential reference name to update.\n * @param value - write-only credential value.\n * @returns an empty object after the value is stored.\n */"
          },
          {
            "kind": "method",
            "name": "remoteUnset",
            "signature": "@Remote('unset') async remoteUnset(refName: string): Promise<Record<never, never>>",
            "summary": "Remove one provider-managed credential through the Native Remote plane.",
            "jsDoc": "/**\n * Remove one provider-managed credential through the Native Remote plane.\n * @param refName - credential reference name to remove.\n * @returns an empty object after the reference is removed.\n */"
          }
        ],
        "types": [
          {
            "name": "ApiKeyRecord",
            "declaration": "export interface ApiKeyRecord {\n    readonly kind: 'api-key';\n    readonly key?: string;\n    readonly env?: Readonly<Record<string, string>>;\n}"
          },
          {
            "name": "Branded",
            "declaration": "export type Branded<B extends string> = string & { readonly [BRAND]: B; };"
          },
          {
            "name": "CredentialInfo",
            "declaration": "export interface CredentialInfo {\n    configured: boolean;\n    source?: string;\n    writable: boolean;\n}"
          },
          {
            "name": "CredentialKey",
            "declaration": "export type CredentialKey = Branded<'CredentialKey'>;"
          },
          {
            "name": "CredentialRecord",
            "declaration": "export type CredentialRecord = ApiKeyRecord | GrantRecord;"
          },
          {
            "name": "CredentialRecordEntry",
            "declaration": "export interface CredentialRecordEntry {\n    key: CredentialKey;\n    kind: CredentialRecord['kind'];\n}"
          },
          {
            "name": "CredentialRecordInfo",
            "declaration": "export interface CredentialRecordInfo {\n    configured: boolean;\n    kind?: CredentialRecord['kind'];\n    writable: boolean;\n}"
          },
          {
            "name": "CredentialRef",
            "declaration": "export type CredentialRef = Branded<'CredentialRef'>;"
          },
          {
            "name": "GrantRecord",
            "declaration": "export interface GrantRecord {\n    readonly kind: 'grant';\n    readonly payload: unknown;\n}"
          },
          {
            "name": "RemoteCredentialsDescription",
            "declaration": "export interface RemoteCredentialsDescription {\n    readonly credentials: Readonly<Record<string, RemoteCredentialView>>;\n}"
          },
          {
            "name": "RemoteCredentialView",
            "declaration": "export interface RemoteCredentialView {\n    readonly configured: boolean;\n    readonly source?: string;\n    readonly writable: boolean;\n}"
          },
          {
            "name": "ResolvedCredential",
            "declaration": "export interface ResolvedCredential {\n    value: string;\n    source: string;\n}"
          }
        ]
      }
    ],
    "events": [
      {
        "description": "Committed change to a stored credential record: a `modifyRecord` that wrote, a `deleteRecord` that removed, or an external edit observed in storage. Separate from `credentials/reference-updated` because the two key grammars are disjoint — a listener that received both on one event could not tell which space a subject belongs to. Listener failures are contained on the same terms as `credentials/reference-updated`.",
        "summary": "Committed change to a stored credential record: a `modifyRecord` that wrote, a `deleteRecord` that removed, or an external edit observed in storage.",
        "tags": [
          {
            "name": "param",
            "argument": "key",
            "comment": "- the record whose stored value changed.",
            "text": "@param key - the record whose stored value changed.\n     *"
          },
          {
            "name": "mode",
            "comment": "emit",
            "text": "@mode emit"
          }
        ],
        "jsDoc": "/**\n * Committed change to a stored credential record: a `modifyRecord` that\n * wrote, a `deleteRecord` that removed, or an external edit observed in\n * storage. Separate from `credentials/reference-updated` because the two key\n * grammars are disjoint — a listener that received both on one event could\n * not tell which space a subject belongs to. Listener failures are\n * contained on the same terms as `credentials/reference-updated`.\n * @param key - the record whose stored value changed.\n * @mode emit\n */",
        "name": "credentials/record-updated",
        "mode": "emit",
        "signature": "'credentials/record-updated'(key: CredentialKey): void"
      },
      {
        "description": "Committed change to a provider-managed credential source: a `set`, an `unset`, or an external edit observed in storage. Ambient process-environment changes are not observable and never emit. Listener failures are contained and logged — a sync throw and an async rejection alike — without changing the committed operation's outcome, except `INVARIANT`-coded failures, which rethrow after every listener ran; that rethrow reaches the emitter only from synchronous listeners, so invariant checks on this event must not be async functions.",
        "summary": "Committed change to a provider-managed credential source: a `set`, an `unset`, or an external edit observed in storage.",
        "tags": [
          {
            "name": "param",
            "argument": "ref",
            "comment": "- the reference whose stored value changed.",
            "text": "@param ref - the reference whose stored value changed.\n     *"
          },
          {
            "name": "mode",
            "comment": "emit",
            "text": "@mode emit"
          }
        ],
        "jsDoc": "/**\n * Committed change to a provider-managed credential source: a `set`, an\n * `unset`, or an external edit observed in storage. Ambient\n * process-environment changes are not observable and never emit. Listener\n * failures are contained and logged — a sync throw and an async rejection\n * alike — without changing the committed operation's outcome, except\n * `INVARIANT`-coded failures, which rethrow after every listener ran;\n * that rethrow reaches the emitter only from synchronous listeners, so\n * invariant checks on this event must not be async functions.\n * @param ref - the reference whose stored value changed.\n * @mode emit\n */",
        "name": "credentials/reference-updated",
        "mode": "emit",
        "signature": "'credentials/reference-updated'(ref: CredentialRef): void"
      }
    ],
    "objects": []
  },
}
