# cinematic-prompt Knowledge Index（深档索引层）

> 深档不默认加载；先看用途→章节→只读对应小节。metadata（loading: on-demand）已标注。

## instructions/templates.md（277 行 · 按需加载）

- 用途：分镜模板库 — ## 一、按时长分类模板（5秒/10秒/15秒/60秒+）；## 二、专题分镜模板（叙事/动作打斗/产品展示/情感冲突/风景旅拍/伪纪录片/视频延长）
- 调用：任务匹配分镜模板主题时读取
- 读取：按时长或类型只读对应小节

## references/movie-styles.md（318 行 · 按需加载）

- 用途：300+ 电影风格词库 — ## 影视风格（60条）；## 画面质感（60条）；## 灯光色调（60条）；## 摄影手法（60条）；## 平台风格（60条）
- 调用：选画风/风格锚定时读取（与 professional-cinematic-image/style-anchor.md 配合）
- 读取：按需只读对应风格类别

## references/storyboard-prompts.md（423 行 · 按需加载）

- 用途：分镜画面提示词库 5000+ — ## 镜头类型（80条）；## 人物动作（80条）；## 情绪氛围（80条）；## 环境设定（80条）；## 时间状态（80条）；## 场景速写
- 调用：写分镜画面描述时读取
- 读取：按镜头/动作/情绪/环境/时间分类只读对应小节

## references/script-templates.md（305 行 · 按需加载）

- 用途：AI 视频脚本分镜模板 300 条
- 调用：写剧本/分镜草稿时读取
- 读取：按需要挑选对应模板

## references/output-template.md（138 行 · 按需加载）

- 用途：输出 markdown 七章模板 — ## 一、角色设定；## 二、场景设定；## 三、完整剧本；## 四、分镜表；## 五、分镜视频提示词；## 六、声音设计；## 七、制作要点
- 调用：组装最终制作全案时读取
- 读取：按章节套用模板结构

## references/image-to-prompt-module.md（200 行 · 按需加载）

- 用途：图片还原提示词生成器 — 上传图片 → 结构化 JSON（subject/expression/face/hair…）+ 100 词内平文本 Prompt
- 调用：用户上传参考图需要还原/复刻时读取
- 读取：按 JSON Schema 章节套用

## references/text-to-prompt-module.md（130 行 · 按需加载）

- 用途：文字转图片提示词公式 — 摄影风格器材/主体特征/面部皮肤/光影构图/负面词 结构化段落
- 调用：文字描述扩写为专业图片提示词时读取
- 读取：按提示词结构公式章节套用

## references/《星光独白》-制作全案-module.md（213 行 · 按需加载）

- 用途：完整示例全案 — 角色设定/场景设定/剧本/分镜表/视频提示词/声音设计全章实际样例
- 调用：需要参考完整制作全案格式时读取
- 读取：按需对照各章节样例

## 加载策略

- 热路径（每次）：SKILL.md + instructions/core.md + instructions/image-prompt.md（必读）
- 按需路径：本索引对应深档的小节
- 禁止：一次读取深档全文（除非用户明确要求）
