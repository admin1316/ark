# 全仓复杂度审计

扫描仓库的生产源码与依赖声明，跳过 `.git`、依赖缓存、构建产物和生成文件。寻找重复 owner、仅转发包装、单实现抽象、死开关、平台已有能力和可移除依赖。

按收益从高到低输出：`标签: 内容。替代方案。[路径]`。结尾给出基于真实命中的可删除文件、行数和依赖数量；无法可靠计数时标记 UNKNOWN，不估算。

这是只读报告。正确性、安全、性能与现有用户数据不在删除建议范围；任何实际删除必须另走对应 Gate。
