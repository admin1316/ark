# 精简债务台账

使用 `rg` 搜索生产源码中的明确标记：`ponytail:`。跳过 `.git`、依赖缓存、构建输出和第三方 vendored 源码。

每项输出：`文件:行号 · 当前简化 · ceiling · 重新评估触发条件`。没有明确 ceiling 或触发条件的标为 `no-trigger`。

默认只读输出；只有用户明确要求落盘时才创建台账文件。没有命中时报告“无 Ponytail 债务标记”。
