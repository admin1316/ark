# 上游效果数据

只显示上游仓库当前公开且可追溯的 benchmark，不把它声称为当前 Ark 的收益。

截至本次集成，上游 README 的 agentic benchmark 报告均值约为：代码行数 -54%、tokens -22%、成本 -20%、时间 -27%；在容易过度实现的单项任务中代码缩减最高约 94%。这些数字必须附来源版本/链接，并说明不是当前仓库测量值。

Ark 自身只接受 before/after 的真实 diff、测试耗时、CPU、内存与行为 Gate；没有基线不得生成节省百分比。
