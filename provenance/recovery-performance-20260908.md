# Ark recovery and performance verification — 2026-09-08

Status: the current native build covers all 72 required endpoint descriptors, but transaction review, test coverage, packaging, and real-App release acceptance remain incomplete. Historical diagnostic-candidate measurements below are not current-source release evidence. Legacy Web checks retain a separate failed result under the approved native-independent release boundary.

## Preserved inputs

- Source before this recovery: `/Users/hui/Ark-Recovery-20260908/source-before-bPgRLb.tar.gz`, SHA256 `e4955cddab2b333ec7b4872f3ca4fb23db1a05662b21e57851d5ca0d1d7e5c69`.
- Existing installed application retained at `/Users/hui/Ark-Recovery-20260908/baseline-bPgRLb/Ark.app`; executable SHA256 `b9e300bbef5ca221825a28a56051b662d3becfa56b126b3b7a0c5fd2725e1863`.
- Source checkout base: `4aa8ef9c3589aba99bf351fd49bbbe5e5725df25` plus the uncommitted recovery diff. These are reconstructed changes, not a recovered original R21 source tree.

## Startup measurement

Both measurements inspect `/Applications/Ark.app/Contents/Resources/runtime` and accept the same 307 physical package identities. No user profile is opened by either measurement.

| Verifier | Wall time |
| --- | --- |
| Installed verifier, one Node syntax-check process per entry | 152,192 ms |
| Source verifier, bounded compile-only VM batches | 4,337 ms |
| Source verifier after Node syntax-boundary correction, repeat run | 4,618 ms |

This measures the runtime-verification phase, not launch-to-interactive time. Package content hashing, dependency reachability, forbidden-package checks, and path checks remain enabled. The 18 runtime-closure tests pass, including invalid syntax, CommonJS/module distinction, nested package type, batch boundaries, non-execution, receipt tampering, and escaped paths. Thirteen syntax-boundary fixtures compare acceptance with the selected Node's own `--check`; this caught and corrected BOM-plus-shebang normalization that would otherwise accept Node-invalid input.

## Native changes and checks

Markdown completions are staged in the existing session/source/token owner and coalesced into one snapshot per 16 ms publication interval. A source change, cancellation, or session switch invalidates pending results. The native contract executable builds; all 40 groups pass, including cancellation after staging. Its synthetic 10-message, 2,048-block hosting probe reports 2,060 rows, 0.189 s initial render in the first run and 0.471 s in the repeat run, and zero bottom-offset error in both. This synthetic probe does not substitute for restored production-history acceptance.

## Diagnostic candidate

- App: `/private/tmp/ark-recovery-20260908-bPgRLb/preview/Ark.app`.
- Bundle ID: `cn.jiuzhangtianmu.industrybrain.candidate.7c463265dbca4c8f`.
- Data home: `/private/tmp/ark-recovery-20260908-bPgRLb/preview-data`.
- Runtime: `/private/tmp/ark-recovery-20260908-bPgRLb/diagnostic-runtime`.
- The supported non-self-contained candidate build path produced this App; deep strict codesign verification passed. It references a preserved R21 runtime copy with the startup and skill overlays. It is not a reproducible release from the reconstructed TypeScript tree.
- First process chain: UI `97021`, launcher `97022`, backend `97049`, listener `127.0.0.1:53986`.
- The user configured the isolated candidate and completed a greeting and `/ponytail 可以用吗`. The gesture remains a short user message; a separate collapsed `skill-invocation` context carries its body. The capability menu shows six studio skills.
- Real interaction checks: eight menu open/close cycles; five Full Access confirmation openings followed by cancellation. No permission elevation was committed by the test.
- During a longer model response, UI samples ranged from 6.3% to 19.9% CPU; backend samples ranged from 6.6% to 13.6%. After cancellation, UI was 0.0% and backend 0.1% in the observed sample.
- A clipboard automation timeout was investigated with a three-second sample. The UI was idle at 0.0% CPU with its main thread waiting in CFRunLoop/Mach messaging. Direct automated non-ASCII keystrokes did not reproduce the intended draft, so that attempt is not Chinese IME acceptance.
- The longer model request diverged into character-counting tool calls despite its instruction not to use tools. The parent stopped that test. It did not establish successful long-task completion.
- Normal Quit removed all three owned processes; the original port returned `ECONNREFUSED`.
- Relaunch restored the test session; the first ready observation was within 18,141 ms of the automation request. Observation latency is included, so this is an upper bound, not an exact first-interactive measurement.
- Second process chain: UI `6470`, launcher `6474`, backend `6498`. A post-restore sample reported 0.0%, 0.1%, and 0.1% CPU respectively.
- The subsequent BOM/shebang verifier correction is source-tested and measured against the installed package tree; the active diagnostic runtime was not modified in place to apply it.

## Source reconstruction and remaining release blockers

Skill admission, provider loading, preset resolution types, Workspace deletion-error identity, and the Skill Remote declaration were restored or corrected using retained runtime code and declarations. Focused behavior tests pass. The build orchestrator compiles current source before bundling and propagates compiler failure; it does not accept an existing lib file or copy an installed runtime as a successful source build.

The complete CI invocation was attempted in an independent source copy with two workers. It failed on missing script registration, version/manifest constraints, and duplicated restored legacy code, then was interrupted before exhaustive coverage continued. The missing application-entrypoint script registration was restored; that verifier still rejects the reconstructed mix of native and legacy launcher classifications. Broader source/compiled-artifact drift remains, particularly in LLM, Workspace, compiler faces, and build manifests.

Source references to `ToolCallId` were mechanically aligned with the retained R21 `CallId` export. Message field names and ID values were unchanged. Nineteen targeted test files passed, with 379 tests. Source aliases for the Remote Agent lookup and event allowlist now avoid silently loading retained `lib/types` JavaScript. The adjacent Remote and session-operation run passed 16 tests and failed five legacy Remote event tests: those expect an `inject` export and event-bridge implementation absent from the retained native entry. These failures remain visible; the native event owner must be resolved before this mixed source tree can pass its complete gates.

The compiler wrapper was also exercised with real TypeScript, not only a stub. TypeScript rejects `--noEmitOnError` as a `--build` CLI argument; the flag was moved to the shared compiler configuration. Four wrapper tests pass, covering stage order, compiler failure, valid compilation, and preservation of an existing output when source has a type error. No bundling follows compiler failure.

The LLM and Agent source projects compile successfully in the isolated validation copy. The Agent project exposed a missing reverse Context identity function; the implementation now admits only the exact live registry entry. The affected Agent test file passes all 19 tests, including absent ownership, a stale same-ID instance, and retirement. The six adjacent Agent/registry files passed their 90 tests before the final fixture correction; none of their source dependencies changed in that correction.

The installed `/Applications/Ark.app` has not been replaced, and no remote Git history has been changed. Production promotion, a self-contained package from fully reconstructed source, full CI, Chinese IME acceptance, and long-history stress closure remain outstanding.

## Implementation continuation

This section records the checkpoint before Team source reconstruction; the 2026-09-09 continuation below supersedes its missing-Team-source finding.

Work continues on `codex/native-recovery-release-20260908`. Host request types, native question-provider registration, CLI readiness and profile reload policy, SDK record predicates, history chunk-row identification, credential metadata exports, and Gateway compiler references were corrected. Separate targeted runs passed 90 interaction/CLI/webserver tests, 219 SDK/goal/instruction tests, 54 history tests, 21 native-event tests, and 13 credential tests; these counts are scoped runs, not a repository-wide total.

Permanent deletion is restored in the shared persistence coordinator, JSONL, and SQLite. An initial backend regression run passed 492 tests with one platform skip. The added deletion selection passed 20 tests; the JSONL boundary and feedback selection passed 24 tests. JSONL uses the existing atomic-write lock and rejects orphan locks rather than guessing at safe reclamation. Real power-loss and orphan-lock recovery acceptance remain outstanding.

A read-only graph audit found 11 native runtime workspaces absent from the Host compiler aggregate. They are included. The initial graph test checked membership but did not reject missing source projects; it was strengthened and now fails on `@deepseek-ai/dsh-agent-team` and `@deepseek-ai/dsh-tool-agent-team`, which contain only retained compiled artifacts. The current recovery and local worktree branches contain no source for these packages. The Workbench compiler project was also restored. These checks exposed previously unchecked Workspace admission methods and mixed Host/Client compiler references. No compiled artifact fallback or test exclusion was used to suppress those failures.

CI configuration changes are local only: standard hosted runners, main-branch triggers, and a required macOS native-contract job. YAML parsing and native-verdict inclusion were checked; no GitHub run, PR, merge, or production replacement has happened for these changes.

The follow-up persistence/feedback/native-build selection passed 533 tests with one platform skip before the native-build guard was strengthened. The strengthened guard's current failure is reported separately above and blocks release. The shared deletion contract, JSONL retry and symbolic-link tests, and code lint passed; these are not full-repository or native candidate acceptance.

## 2026-09-09 source and endpoint continuation

The two Team packages have reconstructed TypeScript implementations, not recovered original source. The real Loader composition exercises task revisions/dependencies, fresh/fork members, durable quiet/wakeup messages, and teardown. Its retained-artifact fold fixtures compare the same synthetic event inputs. Team coverage previously failed at 76.01% statements and 58.58% branches; this is still not a closed release gate.

Settings now owns its five native Remote methods, exact-revision activation settlement, protected domain writes, native document cancellation/path ownership, and namespace retirement. A regression exposed an unsubscribed-but-running callback escaping namespace drain; its tail remains owned until completion. Redaction covers union/intersection/tuple secret positions, parent and field schema defaults, and fail-closed unsupported secret transforms. A real Loader plus file-backed provider snapshot verifies persistence, activation, stale-editor refusal, and unload. The final Settings coverage run passed 173 behavior tests but failed the unchanged per-file threshold: 96.5% statements, 93.14% branches, 96.52% functions, 98.66% lines, with 50 uncovered locations. The broader Settings/Workspace/Todo/Team selection passed 322 tests in 21 files; the two Settings/Team Loader snapshots passed; the native-build guard selection passed 10 tests. These are separate scoped results, not a native-release verdict.

In `/private/tmp/ark-recovery-20260908-bPgRLb/validation-source`, 163 old `lib` directories were moved intact to `/private/tmp/ark-recovery-20260908-bPgRLb/validation-artifacts-before-native-build`. No installed app bundle was copied into the build. The native compiler then emitted 162 source packages and 386 executable entries, followed by 16 generated reflection contributions. Source-only compiler planning has a distinct input identity and cannot certify packlists or built packages; package planning keeps artifact hashing and verification. The Todo projection reuses the existing Session-owned `TodoItem` rather than duplicating the event type. Workspace wire records are publicly exported through its type-only subpath.

Loading the fresh reflection exposed undeclared `zod` dependencies in Settings and Skill; both manifests and the existing lock resolution were corrected. The endpoint audit found 59 of 72 required Native endpoints, with no wrong owners, non-strict results, or duplicate descriptors among those found. Missing: `agentPreset/{copy,list,openDocument,read,remove,select}`, `llm/{models,mutateProvider,providers}`, and `subagent/{history,interrupt,list,prompt}`. The build now treats endpoint gaps as failures; successful TypeScript and reflection emission alone do not establish native readiness.

The complete legacy lint command fails in `experimental/webworker-runtime` on two absent `TypertGateway.wireStream` members. `test:docs` is not registered in this recovered checkout. `doc-sync` was retried on the host after sandbox IPC refusal and remains failed with individual results in `/private/tmp/ark-recovery-20260908-bPgRLb/doc-sync-20260909.log`. The native-independent release aggregate and CI remain unfinished. No PR, merge, self-contained candidate, installed-app replacement, Chinese IME acceptance, or full performance acceptance is claimed. A read-only GitHub fetch still reports `main` at `4aa8ef9c3589aba99bf351fd49bbbe5e5725df25`.

Focused lint and whitespace checks on this batch's source/tests pass. Four touched README/Agent Note pairs were verified consistent. The repository-wide whitespace check reports trailing whitespace in generated `packages/preset/agent-presets/lib/types/preset.{js,d.ts}`; full documentation checks emitted tracked compiled outputs in the source checkout, so those artifacts must not be mistaken for authored changes or silently staged into the repair PR. The original source archive, Git baseline, and preserved application remain available. The installed executable still hashes to `b9e300bbef5ca221825a28a56051b662d3becfa56b126b3b7a0c5fd2725e1863`.

## 2026-09-09 Native wire and delivery continuation

The generated endpoint inventory now covers 71 of the 72 Native-required routes. Only `llm/mutateProvider` remains absent; the build still exits unsuccessfully for that exact gap. This is an endpoint count, not a percentage of project completion. Exhaustive coverage, documentation checks, the complete Loader application, candidate packaging, performance/IME acceptance, CI, and formal replacement remain unclosed.

Preset Remote binding uses `agentPreset` while retaining the `agentPresets` service key. Copy accepts the Native `agentPreset` argument and returns its id; selection delegates to the existing serialized switch and returns the committed id; removal and native document opening supply the Native result shape. User-preset directory opening is implemented with the existing native command utility instead of returning an unfulfilled handoff flag. Shipped presets are refused. Infrastructure errors do not echo native command, filesystem, or composition diagnostics.

Subagent binding uses `subagent` with the existing `subagents` service key. Its Native prompt uses a stable UUID recorded as `subagent-prompt` source metadata. The continuation manager owns retry detection under its existing child lock and excludes inherited fork history. Simultaneous and cold retries return one message id, and conflicting reuse rejects. After acceptance, both the Session flush and the persistence owner's `ensureMaterialized` barrier complete before a durable receipt is returned. Late caller cancellation does not stop this barrier, and an error does not retract the accepted message. The cold-history path passes an expected-parent constraint to the Session history owner, which checks the actual header before presenting page data.

LLM provider and model reads return the Native catalog envelopes, and `llm/discoverModels` now accepts the Native request object and returns `{ models }`. The latter was counted as present by the name-only audit despite its incompatible arguments and return type. Provider catalog failures are isolated, and one-shot keys/raw provider diagnostics are excluded from replies. Thirteen Native-shaped request/result examples were checked against the newly generated codecs; this is an artifact-contract smoke, not a live GUI or transport round trip.

The expanded focused run passed 829 tests in 45 files. After strengthening the actual storage-owner barrier, a targeted run passed 133 tests in three files. One pre-existing fixture had disabled both persistence and query while expecting only a missing-query error; it was split into independent missing-query and missing-persistence cases, preserving both assertions. A real Loader/JSONL/model-substitute snapshot records one initial child turn plus one Native follow-up, exactly one delivered message, and duplicate receipts without further child activation. The first concurrent winner is intentionally not fixed by the API; the snapshot performs one acknowledged initial call before parallel exact retries, while a separate test covers concurrent first calls.

The missing provider writer is not implemented as a sequential settings/credential shortcut. Its retained contract uses the existing credential-record owner for a version-1 grant journal: prepared, credential-staged, settings-applied, credential-applied, then a terminal outcome. It requires endpoint-bound credential references, sibling/foreign-reference denial, exact retry digests, cancellation before durable claim only, and activation settlement after persistence. Restoring it also requires resolving the current Settings-to-Session dependency before adding LLM-to-Settings compiler references, since Session already depends on LLM. No typecheck relaxation or second database is authorized by this recovery.

Final checks for this continuation: 830 behavior tests passed in 45 files, and all three Native Settings/Team/subagent Loader snapshots passed without updating expected output. Focused lint and source whitespace checks passed. Coverage over the six affected runtime implementation files remains failed at the per-file 100% thresholds: 98.7% statements, 97.41% branches, 98.95% functions, and 98.7% lines, with 65 uncovered locations. This does not supersede the separately failing broader Team and Settings coverage scopes. Five touched documentation pairs were checked consistent. Full documentation and lint checks ran in the isolated validation source and remain failed; lint stops at the two legacy `wireStream` errors. The final native build compiles and emits its current-source entries but exits 1 on `llm/mutateProvider`. The 71/72 inventory must not be used as a release success receipt.

## 2026-09-09 frozen-source replay and provider transaction review

The canonical source remains `/Users/hui/ark`; `/tmp/ark-r18` is absent. A frozen input manifest contains 11,029 entries and explicitly excludes 5,237 generated or local-state entries. Its source-tree SHA256 is `28ba245bfc9443cd5d4e6c5a7eb7e0998777cc4431f2793aaf97de4c444c18d5`; the source archive SHA256 is `fed2044289b06b4a7d67f11d79401eabcb2deb6cf1ad39c1d997ff51ba755b76`. A fresh source replay independently passed 830 behavior tests and three Loader snapshots with no workspace `lib` directories before or after the tests and unchanged input hashes. Building that baseline then correctly failed on the sole missing `llm/mutateProvider` endpoint. This immutable baseline predates the provider transaction changes below.

Settings' JSON vocabulary and snapshot parser are independent of Session, removing the LLM-to-Settings-to-Session-to-LLM compiler cycle. The snapshot parser rejects sparse array holes, negative zero and missing set-operation values; parsed path operations no longer require cross-type assertions. LLM's native provider writer uses the existing Settings and Credentials owners, snapshots queued input, stages deterministic credential generations without overwriting the old active reference, waits for Settings activation before removing an old credential, and retains completed retry receipts in the existing grant record. It adds no separate database. The current implementation serializes writes within one LlmRuntime; the retained execution-ownership contract's shared-service identity and independent-resource progress still require independent review.

The working validation tree compiled and emitted the native dependency graph successfully. Its receipt reports 72 required and 72 covered endpoints, with no missing endpoints, wrong owners, non-strict result codecs, or duplicates. Its runtime-plan source digest is `bbc24580ddde7b6bce3f40ba97ab05abec4cfa1422a60414e9067a8a1c992c18`. The transaction suite passed 21 tests using real Loader, file settings and file credentials with synthetic values and an empty launch credential environment. It covers config-only and reference-only changes, endpoint/key pairing, staging and persistence failures, stale/concurrent/repeated writes, cancellation, restart, nested calls, activation failure, secret rejection and generic Settings Remote denial. A broader LLM/Settings run passed 427 tests in 24 files. Focused lint passed on seven changed source/test files. These results do not replace full coverage, an assembled provider-writer snapshot, transport/GUI validation or performance acceptance.

The frozen twelve-file Phase 2 review input has SHA256 `186aefa083efc11205325bc852094a11db672dbc0dad8737d69c707e81f29d88`. Its files match the working validation inputs. The source-only baseline archive, per-file manifests, replay logs, native build receipt and Phase 2 review files are preserved under `/Users/hui/Ark-Recovery-20260908/native-closure-20260909-7qoLnX`, with a checked `BACKUP_RECEIPT.json`. GPT-5.6 Sol High independent read-only review is pending; the scope does not certify release readiness.

The writer refuses grant journals without the verifiable completed-receipt history instead of deleting or upgrading them speculatively. Compatibility with those retained journals is an explicit release blocker. Existing endpoint-generation and execution-ownership notes describe stronger retained behavior and must not be silently rewritten to bless narrower reconstruction. The native-independent release aggregate, broader coverage/documentation failures, self-contained packaging, candidate isolation, three-launch/CPU/IME/long-history checks and CI remain outstanding. A fresh fetch still reports `origin/main` at `4aa8ef9c3589aba99bf351fd49bbbe5e5725df25`; no commits, push, PR, merge or application replacement occurred. The installed executable remains SHA256 `b9e300bbef5ca221825a28a56051b662d3becfa56b126b3b7a0c5fd2725e1863`.

## 2026-09-09 independent review and scoped revision

Sol High returned REVISE for the first frozen transaction implementation. It found missing shared-service execution ownership, completed-receipt replay during an active journal, revision-bound retry digests that could wedge Native, extra operation fields leaking into the durable plan, and unnecessary blocking of unrelated providers. It independently confirmed the legacy journal/status/resume gap as a release blocker. The first review remains preserved at `/private/tmp/ark-provider-review-20260909-7qoLnX/REVIEW_R1.md`; its earlier 427-test result is not a review approval.

The revised implementation keys resource leases by Cordis's original Credentials and Settings service identities, reserves journal then namespace then references, and drains each runtime's accepted work. It rejects other transaction receipts while the current journal is unfinished. Input operations are reconstructed from their legal fields, durable plans reject unsupported operation fields, and exact retry identity omits the caller's refreshed revision while retaining the durable plan's original revision. Real Loader tests mount two isolated LlmRuntime instances over the same storage owners and prove that duplicate writes share ownership. Another regression covers independent namespace progress. Domain protection in Settings is maintained per calling fiber so unloading one runtime cannot erase another's protection.

The final revised run passed 431 tests in 24 LLM/Settings files, including 25 transaction cases. Four-file focused lint passed. Native rebuild exited 0 with all 72 required endpoints covered and no owner, strictness or duplicate failures. The revised twelve-file review snapshot at `/private/tmp/ark-provider-review-r2-20260909-7Rb31K` has scope SHA256 `53bde0a78d071f47d288d1ee0bb7b0d1b675d8fa59ddbc96304b4d7c3db62607`; its source files match the rebuild inputs. A second scoped review is pending. No coverage threshold, full native gate, provider-writer transcript snapshot, compatibility migration, GUI/candidate or production result is claimed. The old-journal/status/resume blocker remains open, and no publication or installed-app replacement is authorized by these scoped successes.

The second review accepted shared-service ownership, active-journal ordering, operation normalization, independent-resource progress and fiber-owned Settings protection. It required one more correction: JSON object insertion order must not distinguish exact retries. The request and plan digests now recursively sort object keys with the standard JSON encoder and preserve array order. Added completed/restart and active-journal tests accept equivalent nested model objects with reversed key insertion order and refuse reordered arrays. The third independent review accepted this specific correction; it did not approve the remaining compatibility work or release.

The final source snapshot `/private/tmp/ark-provider-review-r3-20260909-ktCRSj` has scope SHA256 `f569e8e1f7a80348cfcbcb2c354aa1b757c6d1899fbda87cc73f4519c744ce56`. Its twelve source hashes match the isolated build inputs. The final transaction run passed 27 cases, the final broader run passed 433 tests in 24 files, focused lint passed, and native rebuild exited 0 with 72/72 endpoint descriptors. The LLM README pair was updated and verified consistent. Broader coverage and documentation gates were not rerun or promoted during this blocked review stage. One intermediate native build was intentionally interrupted with exit 130 before final rebuilding; failed dual-runtime fixture attempts used the wrong Loader return/access forms and were corrected to use actual isolated entries, without removing tests or changing their acceptance assertions.

An isolated retained-implementation oracle generated eight valid version-1 journal snapshots for config-only and synthetic-key transactions: prepared, settings-applied and done for the first; prepared, credential-staged, settings-applied, credential-applied and done for the second. The snapshot file `/private/tmp/ark-legacy-provider-oracle-20260909-AjwNvG/LEGACY_V1_JOURNALS.json` has SHA256 `0c470f13fde482852e6ed683b426b426e99929b40c060996182f831212cbab3d`. Only preserved package code and explicitly isolated synthetic Settings/Credentials files were used, with an empty launch credential environment and zero model calls. The exported journal snapshots contain no secret values. This is a differential reference fixture, not a source-build fallback and not legacy recovery acceptance. It confirms that actual retained journals lack the new receipt history; restoring their status/resume and compatibility path remains the concrete blocker before further release gates.
